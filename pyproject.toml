[tool.poetry]
name = "favie-data-schema"
version = "0.0.51-alpha.6"

description = "Favie Data Schema"
authors = ["allenz-srp <allenz@srp.one>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
pydantic = "2.6.4"
pydantic-settings = "2.2.1"
favie_common = {git = "git+https://github.com/SerendipityOneInc/favie_common.git", rev="0.1.5.alpha.0"}

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.isort]
profile = "black"